<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pruebas.diseno">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".editar_Sintomas" />
        <activity android:name=".sintomas_agregar" />
        <activity android:name=".enfermedades_lista" />
        <activity android:name=".editar_enfermedades" />
        <activity android:name=".enfermedades_Agregar" />
        <activity android:name=".medicamentos_lista" />
        <activity android:name=".editar_medicamentos" />
        <activity android:name=".medicamentos_agregar" />
        <activity android:name=".asignar_sintoma_enfermedad"></activity>
    </application>

</manifest>